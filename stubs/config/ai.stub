/*
|--------------------------------------------------------------------------
| AI Configuration
|--------------------------------------------------------------------------
|
| This file contains the configuration for the AI package. You can configure
| multiple AI providers and switch between them as needed.
|
*/

import { defineConfig } from '@adonisjs/core/app'

export default defineConfig({
  /*
  |--------------------------------------------------------------------------
  | Default Provider
  |--------------------------------------------------------------------------
  |
  | The default AI provider to use when no specific provider is specified.
  | Must be one of the providers defined in the "providers" section below.
  |
  */
  default: 'openai',

  /*
  |--------------------------------------------------------------------------
  | AI Providers
  |--------------------------------------------------------------------------
  |
  | Configure your AI providers here. Each provider must have a unique name
  | and specify the driver to use along with its configuration.
  |
  | Available drivers:
  | - openai: Uses OpenAI's API
  | - gemini: Uses Google's Gemini API
  |
  */
  providers: {
    openai: {
      driver: 'openai',
      config: {
        apiKey: env('OPENAI_API_KEY'),
        model: 'gpt-3.5-turbo',
        maxTokens: 1000,
        temperature: 0.7,
        organization: env('OPENAI_ORGANIZATION'),
      },
    },

    gemini: {
      driver: 'gemini',
      config: {
        apiKey: env('GEMINI_API_KEY'),
        model: 'gemini-pro',
        maxTokens: 1000,
        temperature: 0.7,
      },
    },
  },
})